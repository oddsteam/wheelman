<script>
  init();

  async function init() {
    await liff.init({ liffId: "<%= ENV['LINE_LIFF_ID'] %>" });

    if (liff.isLoggedIn()) {
      window.location.href = "/events";
    } else {
      window.location.href = "/login";
    }
  }
</script>